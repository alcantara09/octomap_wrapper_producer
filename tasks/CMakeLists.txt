# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(octomap_wrapper_producerTaskLib)
ADD_LIBRARY(${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_NAME} SHARED 
    ${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_SOURCES})
add_dependencies(${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${OCTOMAP_WRAPPER_PRODUCER_TASKLIB_HEADERS}
    DESTINATION include/orocos/octomap_wrapper_producer)

